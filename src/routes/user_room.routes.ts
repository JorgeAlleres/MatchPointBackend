import { Router } from "express";

const router = Router()

/*router.post('/join', UserRoomController.joinRoom);
router.post('/leave', UserRoomController.leaveRoom);
router.get('/room/:idRoom/users', UserRoomController.getRoomUsers);
router.get('/room/:idRoom/count', UserRoomController.getRoomUserCount);*/

export default router;